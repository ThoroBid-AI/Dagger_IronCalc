# Pipelines

This directory contains oracle capture and conformance runbooks.

Current state:
- Oracle/conformance workflow is documented across `scripts/README.md` and `specs/planning/`.
- Add pipeline-specific runbooks here as they are finalized.
