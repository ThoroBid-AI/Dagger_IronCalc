# Unsupported Functions

Track functions intentionally unsupported or deferred for later implementation.

Current state:
- Use `specs/reports/ironcalc_missing_but_in_hf.csv` for the active parity backlog.
- Move items here only when explicitly marked unsupported/deferred.
