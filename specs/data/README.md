# Data (Legacy)

This folder contains legacy data files used by older batch-based tooling.
These are **not required** for the current Lua + HyperFormula workflow but are
kept to avoid breaking older scripts.

Files:
- `function_batches.csv`
- `function_complexity_map.csv`
- `function_io_shape.csv`
