# Planning Archive

This directory stores historical planning snapshots.

Notes:
- Files under dated subdirectories are archival and may include legacy commands/paths.
- Active execution documents live in `specs/planning/` (non-archive).
- Use `specs/planning/function_implementation_plan.md` as the current entry point.
